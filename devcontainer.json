{
  "name": "CAISO Market Intelligence",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "postCreateCommand": "pip install -r requirements.txt",
  "forwardPorts": [
    8000
  ],
  "portsAttributes": {
    "8000": {
      "label": "CAISO Market Intelligence",
      "onAutoForward": "openPreview"
    }
  },
  "postStartCommand": "bash -lc 'if [ -n \"$OPENAI_API_KEY\" ]; then (uvicorn app:app --host 0.0.0.0 --port 8000 >/tmp/caiso-market-intelligence.log 2>&1 &) ; else echo \"OPENAI_API_KEY Codespaces secret is not configured.\"; fi'"
}