# Preferred: deploy the existing V4 FastAPI service and route the workspace to it.
MARKET_INTELLIGENCE_API_URL=

# Optional current web research through the OpenAI Responses API.
# Store this only as a hosted secret. Never expose it to browser code.
OPENAI_API_KEY=
